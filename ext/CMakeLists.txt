cmake_minimum_required(VERSION 3.14)
project(tinyphp-ext LANGUAGES C)

# ── 第三方库 ──
# 添加新库时，只需在 ext/ 下建目录并加一行 add_subdirectory
# add_subdirectory(openssl)
# add_subdirectory(curl)
# add_subdirectory(sqlite)
